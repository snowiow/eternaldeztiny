CREATE TABLE account (
id INT NOT NULL auto_increment,
name VARCHAR(64) NOT NULL UNIQUE,
userhash VARCHAR(64) NOT NULL,
password VARCHAR(128) NOT NULL,
email VARCHAR(128) NOT NULL UNIQUE,
role INT NOT NULL,
avatar VARCHAR(128),
date_registered TIMESTAMP NOT NULL,
mini VARCHAR(64),
PRIMARY KEY (id),
FOREIGN KEY (warstats_id) REFERENCES warstats(id) ON DELETE CASCADE
);


ALTER TABLE account ADD COLUMN warstats_id INT, ADD FOREIGN KEY (warstats_id) REFERENCES warstats(id) ON DELETE CASCADE;
